Để site tối ưu hơn, bạn có thể tự thêm webfont của riêng bạn vào thư mục này
với tên `inter.woff2`, sau đó cập nhật CSS nếu muốn.

Project hiện dùng system font stack mặc định nên vẫn chạy bình thường
ngay cả khi không có file font riêng.
